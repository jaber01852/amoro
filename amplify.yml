version: 1
frontend:
  phases:
    preBuild:
      commands:
        - flutter channel stable
        - flutter upgrade
        - flutter pub get
    build:
      commands:
        - flutter build web --release
  artifacts:
    baseDirectory: build/web
    files:
      - "**/*"
  cache:
    paths:
      - .pub-cache
